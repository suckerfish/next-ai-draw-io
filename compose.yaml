services:
  next-ai-drawio:
    build:
      context: .
      dockerfile: Dockerfile
    image: next-ai-drawio:latest
    pull_policy: build
    container_name: next-ai-drawio
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=${PORT:-6002}
      - HOSTNAME=0.0.0.0
    env_file:
      - .env
    ports:
      - "${PORT:-6002}:${PORT:-6002}"
    # Override CMD based on NODE_ENV
    command: >
      sh -c "if [ \"$NODE_ENV\" = 'production' ]; then
               npm start -- --port ${PORT:-6002};
             else
               npm run dev;
             fi"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${PORT:-6002}/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    # Resource limits optimized for development workstation
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - my_network

networks:
  my_network:
    external: true
    name: my_network
